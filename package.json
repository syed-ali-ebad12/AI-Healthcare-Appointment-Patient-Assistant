{
  "name": "carely-healthcare-assistant",
  "version": "1.0.0",
  "private": true,
  "description": "Carely healthcare appointment assistant with Express, PostgreSQL, JWT RBAC, and notifications",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "db:init": "node scripts/init-db.js",
    "db:seed": "node scripts/seed.js",
    "check": "node --check server.js && node --check app.js && node --check scripts/init-db.js && node --check scripts/seed.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "node-cron": "^3.0.3",
    "nodemailer": "^6.9.14",
    "pg": "^8.12.0",
    "twilio": "^5.2.3"
  }
}
